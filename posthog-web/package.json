{
  "name": "posthog-web",
  "version": "2.0.0-alpha",
  "scripts": {
    "build": "rm -rf ./dist & tsc -p . && mv ./dist/src/ ./dist-out && rm -rf ./dist && mv ./dist-out ./dist"
  },
  "dependencies": {
    "posthog-core": "2.0.0-alpha"
  }
}
